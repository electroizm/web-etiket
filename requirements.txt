Django==5.2.16
python-dotenv==1.0.1
supabase==2.5.0
PyJWT==2.8.0
cryptography==44.0.3
gunicorn==23.0.0
whitenoise==6.7.0

# Scraper (catalog app) — Python 3.13 wheel'i olan sürümler
aiohttp==3.10.11
beautifulsoup4==4.12.3
lxml==5.3.0
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# PDF etiket üretimi (catalog/services/etiket_pdf.py)
reportlab>=4.2.0
qrcode>=7.4.2
pillow>=10.0.0
requests>=2.31.0

# Hata izleme (settings.py — SENTRY_DSN doluysa aktif)
sentry-sdk[django]>=2.0

# AI ajan (bot/ajan.py) — sağlayıcı-bağımsız model çağrısı (Gemini/Claude/...)
litellm>=1.60

# Saat dilimi veritabanı — bot/kota.py Google'ın kota gününü Pasifik gece
# yarısına göre hesaplar (yaz/kış saati otomatik). Linux'ta sistemde vardır ama
# imaja güvenmiyoruz: yanlış hesap "kota bitti" işaretini yanlış saatte açar.
tzdata>=2024.1
